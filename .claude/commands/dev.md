---
allowed-tools: Bash(bun:*)
---

# /dev — Start Dev Server

Start the TaWTUI development server with hot reload.

## Command

```bash
bun run start:dev
```

This runs `bun run --watch src/main.ts`, which watches for file changes and automatically restarts.
